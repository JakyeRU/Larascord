This error usually occurs when the variable `LARASCORD_CLIENT_SECRET` in the `.env` file is invalid.

Note that `LARASCORD_CLIENT_SECRET` is not the access token. This secret can be found in the `OAuth2` tab of the application.